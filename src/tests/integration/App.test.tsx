import { describe, it } from "vitest";
// import { render, screen } from "@testing-library/react";
//
// import App from "../../App";
// import MainWrapper from "../wrappers/main";

describe("App Component", () => {
  it("renders user name from tRPC API", async () => {
    //render(<App />, { wrapper: MainWrapper });
    //expect(screen.getByText("Interactive")).toBeInTheDocument();
    //const button = screen.getByTestId("check-all");
    // expect(screen.getByText(/count is 0/i)).toBeInTheDocument();
    //fireEvent.click(button);
    // expect(screen.getByText(/count is 1/i)).toBeInTheDocument();
    // await waitFor(
    //   () => expect(screen.getByText(/Monitor/i)).toBeInTheDocument(),
    //   { timeout: 5000 }, // Increase timeout for real server
    // );
  });
});
